# Visual Algorithm Designer Planning Archive

This file used to contain an early provider-specific development plan. That plan is superseded and intentionally removed from the public pre-alpha surface.

The maintained public direction is now:

- pre-alpha SecuredMe Education school tool;
- official school routes through Codex/OpenAI or Antigravity/Gemini browser-authenticated workflows;
- deterministic local guidance when no approved external school assistant is attached;
- no required classroom `.env`, raw token, local model secret, or legacy provider route;
- training-only algorithm design, not automation for theft, abuse, bypass, or criminal use.

Use `README.md`, `SCHOOL_TOOL_GOVERNANCE.md`, and `AGENTS.md` as the current public contract.