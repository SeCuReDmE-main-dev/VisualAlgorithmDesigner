# Hero Book specialist design provenance

The VAD specialist follows its product-specific Stitch sources under `securedme-site/assets/landing/secureme.ca-product/education/VAD/desing/stitch_vad_landing_page_designer/stitch_vad_landing_page_designer`. The dark companion surface uses `premium_cyber_heritage_education/DESIGN.md`: background `#101221`, primary cyan `#a5e7ff`, action cyan `#00d2ff`, violet review `#bd00ff`, and Inter/JetBrains Mono. The light workbench may instead use `cyber_heritage_workbench/DESIGN.md` without changing the hero identity.

The banner is deliberately an accessible text-first fallback with high contrast. It changes only the VAD specialist persona; it does not replace the AlgoQuest Hero Book identity or create a second progression store.
